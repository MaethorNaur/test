name: t
on:
  workflow_dispatch:
    inputs:
      message:
        type: string
        required: true
jobs:
  p:
    runs-on: ubuntu-latest
    steps:
      - run: |
          A="${{join(fromJSON(inputs.message),'\n')}}"
          echo $A > a.txt
          cat a.txt
